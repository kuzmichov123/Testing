import React from "react";

export const Appoinments = ({ customer }) => (
    <div>{customer.firstName}</div>
);

export const AppointmentsDayView = () => (
    <div id="appointmentsDayView"></div>
)